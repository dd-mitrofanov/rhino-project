# Managed by Ansible — Bearer must match SUBSCRIPTION_API_TOKEN on the bot
XRAY_SYNC_WEBHOOK_URL=http://{{ hostvars[groups['telegram_bot'] | first]['ansible_host'] }}:{{ subscription_api_internal_port }}/internal/xray-sync
XRAY_SYNC_WEBHOOK_TOKEN={{ vault_subscription_api_token }}
